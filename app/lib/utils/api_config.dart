// 이 파일은 더 이상 사용되지 않습니다. 
// app/lib/utils/api_constants.dart를 대신 사용하세요.
// 이전 코드와의 호환성을 위해 ApiConfig 클래스를 유지합니다.

import 'api_constants.dart';

class ApiConfig {
  // API 기본 URL - ApiConstants로 대체됨
  static const String baseUrl = ApiConstants.baseUrl;
  
  // API 키 - ApiConstants로 대체됨
  static const String apiKey = ApiConstants.apiKey;
  
  // 기타 API 설정 - ApiConstants로 대체됨
  static const int defaultTimeout = ApiConstants.defaultTimeout;
  static const bool loggingEnabled = ApiConstants.loggingEnabled;
} 